<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <title>Открыть чат поддержки в Telegram</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root { color-scheme: light dark; }
    html,body{height:100%}
    body{
      margin:0; display:grid; place-items:center; font-family:system-ui,-apple-system,Segoe UI,Roboto,Inter,Arial,sans-serif;
      background:canvas; color:canvastext
    }
    .card{max-width:560px; padding:24px; border:1px solid color-mix(in oklab, canvastext 12%, transparent); border-radius:16px}
    h1{margin:0 0 8px;font-size:20px}
    p{margin:8px 0;color:color-mix(in oklab, canvastext 70%, transparent)}
    .row{display:flex; gap:8px; margin-top:16px; flex-wrap:wrap}
    .btn{
      appearance:none; border:1px solid color-mix(in oklab, canvastext 18%, transparent);
      background: color-mix(in oklab, canvas 92%, canvastext 8%);
      color: canvastext; padding:10px 14px; border-radius:12px; text-decoration:none; font-weight:600; cursor:pointer
    }
    .btn:hover{background: color-mix(in oklab, canvas 88%, canvastext 12%)}
    .muted{font-size:13px;opacity:.7}
    code{padding:2px 6px;border-radius:6px;background:color-mix(in oklab, canvastext 10%, transparent)}
  </style>
</head>
<body>
  <div class="card">
    <h1>Открыть чат с поддержкой</h1>
    <p>Нажми кнопку ниже — мы откроем чат в приложении Telegram. Если приложение не установлено, откроется веб-страница t.me.</p>

    <div class="row">
      <a id="open" class="btn" href="https://t.me/emigrate_support" rel="noopener">💬 Открыть чат</a>
    </div>

    <p class="muted">Чат с <code>@emigrate_support</code> (ID <code>7437456248</code>).</p>
  </div>

  <script>
    // Определяем платформу и подставляем лучший линк
    (function () {
      const ua = navigator.userAgent.toLowerCase();
      const isAndroid = ua.includes('android');
      const isIOS = /iphone|ipad|ipod/.test(ua);
      const anchor = document.getElementById('open');

      if (isAndroid) {
        // Надёжный способ для Android: intent:// со схемой tg
        // Откроет Telegram, а если нет — выведет установку через Play Store
        anchor.href = 'intent://resolve?domain=emigrate_support#Intent;scheme=tg;package=org.telegram.messenger;end';
      } else {
        // Для iOS и Desktop даём https — браузер корректно предложит открыть приложение
        anchor.href = 'https://t.me/emigrate_support';
      }

      // Если очень хочется «глубже» — можно добавить вторую кнопку tg:// только для Desktop-клиентов:
      // но чтобы не провоцировать скачивание в некоторых браузерах, лучше не подменять href на tg://
    })();
  </script>
</body>
</html>
