<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <title>Открыть чат поддержки в Telegram</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root { color-scheme: light dark; }
    html,body{height:100%}
    body{
      margin:0; display:grid; place-items:center; font-family:system-ui,-apple-system,Segoe UI,Roboto,Inter,Arial,sans-serif;
      background:canvas; color:canvastext
    }
    .card{max-width:560px; padding:24px; border:1px solid color-mix(in oklab, canvastext 12%, transparent); border-radius:16px}
    h1{margin:0 0 8px;font-size:20px}
    p{margin:8px 0;color:color-mix(in oklab, canvastext 70%, transparent)}
    .row{display:flex; gap:8px; margin-top:16px; flex-wrap:wrap}
    .btn{
      appearance:none; border:1px solid color-mix(in oklab, canvastext 18%, transparent);
      background: color-mix(in oklab, canvas 92%, canvastext 8%);
      color: canvastext; padding:10px 14px; border-radius:12px; text-decoration:none; font-weight:600; cursor:pointer
    }
    .btn:hover{background: color-mix(in oklab, canvas 88%, canvastext 12%)}
    code{padding:2px 6px;border-radius:6px;background:color-mix(in oklab, canvastext 10%, transparent)}
    .muted{font-size:13px;opacity:.7}
  </style>
</head>
<body>
  <div class="card">
    <h1>Открыть чат с поддержкой</h1>
    <p>Нажми кнопку ниже — чат откроется в установленном приложении Telegram. Если приложение не установлено, используйте ссылку t.me.</p>

    <div class="row">
      <button id="open-tg" class="btn">💬 Открыть в Telegram</button>
      <a id="open-web" class="btn" href="https://t.me/emigrate_support" rel="noopener">🌐 Открыть через t.me</a>
    </div>

    <p class="muted">Цель: открыть личный чат с <code>@emigrate_support</code> (ID <code>7437456248</code>).</p>
  </div>

  <script>
    const tgById = "tg://user?id=7437456248";
    const tgByUsername = "tg://resolve?domain=emigrate_support";
    const webFallback = "https://t.me/emigrate_support";

    document.getElementById("open-tg").addEventListener("click", () => {
      // Пробуем по ID
      window.location.href = tgById;

      // Через короткую паузу — по username (если по ID не сработало)
      setTimeout(() => { window.location.href = tgByUsername; }, 700);

      // Финальный фолбэк — веб
      setTimeout(() => { window.location.href = webFallback; }, 1400);
    });
  </script>
</body>
</html>
